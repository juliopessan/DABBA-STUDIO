# Agents

Cada agente do DPABB-Framework (@discovery, @prd, @architect, @backlog,
@business-case) vira um módulo aqui, portando a persona e o pipeline de
`../../../DPABB-Framework/CLAUDE.md` para TypeScript.

Ainda não implementado — próximo passo depois do scaffold.
